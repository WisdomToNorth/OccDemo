{
    "name": "Linux",
    "remoteUser": "root",
    "remoteEnv": {
        "PATH": "/home/linuxbrew/.linuxbrew/bin:$PATH"
    },
    "preCreateCommand": "sudo apt-get update && sudo apt install build-essential procps curl file git qtbase5-dev qtbase5-dev-tools"
}
